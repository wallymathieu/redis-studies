version: 1.0.{build}
configuration: Release
before_build:
  - nuget restore redis-studies.sln
build:
  project: redis-studies.sln
  verbosity: minimal
  publish_nuget: false
nuget:
  account_feed: false
  project_feed: false
  disable_publish_on_pr: true
